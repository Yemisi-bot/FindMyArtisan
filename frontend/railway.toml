[service]
name = "findmyartisan-web"

[build]
buildCommand = "npm run build"

[deploy]
startCommand = "npx vite preview --host 0.0.0.0 --port ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 15
restartPolicyType = "ON_FAILURE"
